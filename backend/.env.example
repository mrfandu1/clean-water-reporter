# Local Environment Variables for Aiven MySQL
# Copy these values to .env file (not tracked by git) to test with Aiven locally

DB_HOST=your-aiven-host.aivencloud.com
DB_PORT=24347
DB_NAME=defaultdb
DB_USERNAME=avnadmin
DB_PASSWORD=your-aiven-password-here
DB_SSL=true
PORT=8080

# Note: For local development with local MySQL, comment out the above
# and the application.properties will use the default values:
# localhost:3306/clean_water_db with root/1234
